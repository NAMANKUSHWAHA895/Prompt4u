package com.example.data.local

/**
 * SeedData for Prompt4u.
 * Preset posts removed as per user instruction:
 * "remove all preset post and make that if admin post new prompts they are visible to all users on the app"
 */
object SeedData {
    val initialPosts: List<PostEntity> = emptyList()
}
